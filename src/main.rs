mod done;

/// main func
fn main() {
  done::numbers();
  done::scope();
  done::match_and_memory();
  done::string_loop();
  done::enums();
  done::union();
  done::handle_none();
  done::array();
  done::slice();
  done::tupple();
  done::pattern_matching();
  done::generics();
  done::vectors();
  done::hash_map();
  done::hash_set();
  done::strings();

  println!("-------------");
}
